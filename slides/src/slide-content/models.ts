export interface LinkItem {
  label: string;
  url: string;
}

export interface Speaker {
  name: string;
  twitter: string;
  bio: string;
  pictureUrl: string;
}

export interface TsFeature {
  tsDocLink: string;
  featureName: string;
  index: number;
}
